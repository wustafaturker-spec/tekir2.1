package com.ut.tekir.billing.entity;

/**
 * Types of usage metrics tracked per tenant.
 * WARNING: Do NOT reorder.
 */
public enum UsageType {
    USERS,
    INVOICES,
    PRODUCTS,
    CONTACTS,
    STORAGE_MB,
    API_CALLS
}
