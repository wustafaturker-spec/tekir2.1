package com.ut.tekir.common.enums;

/**
 * UBL-TR XML generation mode.
 */
public enum UblGenerationMode {
    /** Application generates UBL-TR XML and sends to provider */
    APP_SIDE,
    /** Provider generates UBL-TR XML from structured data */
    PROVIDER_SIDE
}
